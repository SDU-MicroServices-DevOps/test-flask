apiVersion: apps/v1
kind: Deployment
metadata:
  name: hello-world-deployment
spec:
  replicas: 2
  template:
    metadata:
      labels:
        app: flask-hello-world
    spec:
      containers:
      - name: flask-container
        image: flask-image:latest